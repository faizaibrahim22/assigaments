#!/use/bin/env node
let  tab:number =5 
for(let i:number = 1;i<=10;i++){
   console.log(`${tab} * ${i} = ${tab*i}`)
}
let table:number =4
for(let t:number = 1;t<=20;t++){
  console.log(`${table} * ${t} ${table*t}`)
}

